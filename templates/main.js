use strict;

(function() {

  const id = helpers.id;
  const qs = helpers.qs;
  const qsa = helpers.qsa;
  const gen = helpers.gen;

  window.addEventListener("load", init);

  function init() {
    
  }

})();
